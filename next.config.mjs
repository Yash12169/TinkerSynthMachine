/** @type {import('next').NextConfig} */
const nextConfig = {
    typescript: {
      // Set this to true to ignore TypeScript errors during the build process
      ignoreBuildErrors: true,
    },
  };
  
  export default nextConfig;
  